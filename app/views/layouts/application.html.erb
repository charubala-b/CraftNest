<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Project1" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="current-user-id" content="<%= current_user ? current_user.id : '' %>">

    <%= yield :head %>

  <!-- Add Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <%= javascript_importmap_tags %>
  </head>

<body>
  
  <% unless devise_controller? && (controller_name == "sessions" && action_name == "new") || 
             (controller_name == "registrations" && action_name.in?(%w[new create])) %>
  <% if user_signed_in? %>
    <div style="position: fixed; top: 20px; right: 20px; z-index: 1000;">
      <%= button_to "Logout", destroy_user_session_path, method: :delete,
          class: "btn btn-danger", style: "padding: 8px 16px; font-weight: bold; border-radius: 6px;" %>
    </div>
  <% end %>
<% end %>

  <%= yield %>
</body>


  
</html>
